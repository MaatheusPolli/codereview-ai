## Structure
- Proper indentation (2 or 4 spaces)
- Use meaningful tag names
- Ensure all tags are properly closed

## Security
- No hardcoded credentials (passwords, API keys) in config
- No local/dev URLs in production configurations
- Encrypt sensitive data in configuration files

## Standards
- Use attributes for metadata and elements for data
- Include schema definitions where possible
- Version your configuration files
